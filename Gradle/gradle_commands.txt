Траблы:

- Запускать команды gradle нужно так: открыть терминал, gradlew.bat <command>

- Если будет ругаться, что не может выделить память под heap, то нужно уменьшить
  требования к размеру heap в файле gradle.properties и поставить там 'org.gradle.jvmargs=-Xmx512m'